export class Chip {
  label: string;
  background: string;
  font: string;

  constructor(label: string, background: string, font: string) {
    this.label = label;
    this.background = background;
    this.font = font;
  }
}
